<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".workout.WorkoutSessionActivity"
            android:exported="false"
            android:theme="@style/WorkoutSessionTheme" />

        <service
            android:name=".workout.WorkoutTimerService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <receiver
            android:name=".notify.SummaryDismissReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.DueReminderAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.WindowStartAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.DueReminderDismissReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.DueReminderActionReceiver"
            android:exported="false" />

        <!-- Not directBootAware, and no LOCKED_BOOT_COMPLETED - see the real crash writeup on
             this in CLAUDE.md's "Native notifications" section: BOOT_COMPLETED alone still fires
             the moment the device is usable (after first unlock post-reboot), which is early
             enough, and it's the only variant where SharedPreferences (credential-encrypted by
             default) are actually readable. -->
        <receiver
            android:name=".notify.DueReminderBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notify.ExtraReminderAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.ExtraReminderActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.ExtraReminderBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notify.RescheduleReminderAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.RescheduleReminderActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.RescheduleReminderBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notify.DailyDigestAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.DailyDigestBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <service
            android:name=".notify.BackgroundSyncService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the app process alive in the background so scheduled reminder and summary notifications stay up to date without requiring the user to reopen the app." />
        </service>

        <receiver
            android:name=".notify.BackgroundSyncActionReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <!-- Android 16 (API 36) requires a `health`-typed foreground service to hold at least one of
         ACTIVITY_RECOGNITION/HIGH_SAMPLING_RATE_SENSORS/a Health Connect read permission *in
         addition to* FOREGROUND_SERVICE_HEALTH itself, confirmed from a real device's crash log -
         see the doc comment on WorkoutSessionActivity.startTimerServiceOncePermitted(). This is a
         dangerous/runtime permission on API 29+, requested there before ever starting the
         service. ACTIVITY_RECOGNITION fits this app's actual purpose (a workout tracker) far
         better than the health-metric-reading alternatives, which this app has no other use for. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- For BackgroundSyncService. Uses `specialUse`, not the more semantically-obvious
         `dataSync`, because Android 15+ (this app's targetSdkVersion) caps dataSync/
         mediaProcessing foreground services at 6 cumulative hours per 24-hour period while
         backgrounded - which would silently kill this exact feature (staying alive for many
         hours while backgrounded) a few hours into any day the app isn't reopened. Confirmed
         against Android's official docs rather than assumed - see BackgroundSyncService.kt. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- For DueReminderBootReceiver - declared explicitly rather than relying on the merge from
         @capacitor/local-notifications' own manifest, since that plugin's contract shouldn't be
         load-bearing for a different notification type's boot survival. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Non-runtime permission (no request dialog), reserved for the workout timer's Android 16+
         "promoted"/live-update notification once the exact API is confirmed - see the doc
         comment on WorkoutTimerService.buildProgressStyleNotification() for why that part is
         deferred. Harmless to declare now even though nothing calls it yet. -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <!-- For UpdateInstallerPlugin - required on Android 8+ for this app to request installing a
         package at all. Not itself a runtime prompt: the first time the install intent actually
         fires, Android shows a one-time "allow installs from Daily Routines" settings toggle if
         not already granted, then proceeds normally on every install after that. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- For UpdateInstallerPlugin's DownloadManager.Request.VISIBILITY_HIDDEN - a real crash
         found via a user's on-device bug report: without this, DownloadManager.enqueue() throws
         SecurityException("Invalid value for visibility: 2") unconditionally, since hiding a
         download's own notification is gated behind this permission (confirmed against AOSP's
         DownloadProvider manifest source: protectionLevel="normal", so a plain manifest
         declaration is sufficient - no runtime prompt). -->
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
</manifest>
